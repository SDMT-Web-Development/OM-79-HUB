@using OM_79_HUB.Models.DB.OM79Hub
@model IEnumerable<UserData>

@{
    ViewData["Title"] = "AdminIndex";
}

<h1>AdminIndex</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ENumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.ENumber)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.OMId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OMId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OMId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
